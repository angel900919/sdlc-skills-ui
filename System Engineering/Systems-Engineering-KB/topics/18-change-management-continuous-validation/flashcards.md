# Flashcards — Change Management & Continuous Validation

| ID | Front | Back | Tags |
|---|---|---|---|
| q-18-001 | What does change management ensure about modifications to a system? | They are evaluated, approved, and documented to prevent unintended consequences — maintaining integrity, traceability, and compliance.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-002 | Define impact analysis. | Evaluating how a proposed change affects system performance, cost, schedule, and dependencies, to enable informed decisions before approval.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-003 | List the 5 steps of impact analysis in order. | {{c1::Identify change scope}} → {{c2::Assess risks and dependencies}} → {{c3::Estimate cost and schedule impact}} → {{c4::Evaluate compliance and safety risks}} → {{c5::Review stakeholder input}}<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, cloze |
| q-18-004 | Impact-analysis step that asks "what components or subsystems are affected?" | Step 1: Identify change scope.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-005 | Which impact-analysis step catches an accessibility-compliance problem? | Step 4: Evaluate compliance and safety risks.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-006 | List the 6 phases of the change control process in order. | {{c1::Change Request Submission}} → {{c2::Initial Review}} → {{c3::Impact Analysis}} → {{c4::Approval Process (CCB)}} → {{c5::Implementation & Testing}} → {{c6::Documentation & Communication}}<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, cloze |
| q-18-007 | In the change control process, who makes the final approval decision, and at which phase? | The Change Control Board (CCB) or relevant authority, at phase 4 (Approval Process).<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-008 | What is the Initial Review phase checking? | Feasibility, superficially: is this possible — yes or no?<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-009 | What tracks the change control process, and what does it prevent? | Configuration management software (CMS) — it prevents unauthorized modifications and ensures each change is documented (version control + compliance).<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-010 | Which configuration-management tool is named for ensuring regulatory compliance in big companies? | IBM DOORS.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-011 | Name change-management tools for Agile software teams. | JIRA, Azure DevOps, GitHub/GitLab (git-based).<br><sub>(source: m4-changemgmt)</sub> | topic::change-management-continuous-validation, recall |
| q-18-012 | Name change-management tools for complex systems engineering. | IBM Engineering Workflow Management (EWM), Windchill, Helix ALM (Perforce).<br><sub>(source: m4-changemgmt)</sub> | topic::change-management-continuous-validation, recall |
| q-18-013 | Name change-management tools for enterprise IT change control and for risk/impact analysis. | IT: ServiceNow, BMC Remedy. Risk/impact: Ansys ModelCenter, SAP CTS.<br><sub>(source: m4-changemgmt)</sub> | topic::change-management-continuous-validation, recall |
| q-18-014 | Why do so many change-management tools exist, and what's the rule for choosing? | Different industries/teams/workflows have unique needs; "no single tool fits all" — choose by process complexity, compliance needs, and collaboration model.<br><sub>(source: m4-changemgmt)</sub> | topic::change-management-continuous-validation, recall |
| q-18-015 | Define continuous testing. | Integrating automated and manual testing into every phase of the lifecycle (not an end-of-cycle phase) for rapid, ongoing feedback so defects are caught as soon as introduced.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-016 | List the 5 principles of continuous testing. | {{c1::Shift-Left Testing}}, {{c2::Test Automation in CI/CD}}, {{c3::"Fail Fast" Feedback}}, {{c4::Environment Consistency}}, {{c5::Service Virtualization}}<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, cloze |
| q-18-017 | What is shift-left testing and what does it buy? | Defining/automating tests before or alongside code (moving quality checks earlier); it identifies and resolves defects early to prevent costly rework.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-018 | What does "fail-fast" feedback achieve? | Immediate failure reporting prevents defect accumulation, reduces debugging scope, and speeds resolution.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-019 | What is service virtualization, and when is it used? | Lightweight simulators/mocks stand in for dependent services or hardware that aren't yet available, so tests run without blocking.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-020 | What is "environment consistency" in continuous testing? | Provisioning test environments "as code" (containers, VMs) so tests run against known, reproducible configurations.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-021 | Name the 5 Agile test types from smallest to largest scope. | {{c1::Unit}}, {{c2::Integration}}, {{c3::System}}, {{c4::Regression}}, {{c5::User Acceptance Testing (UAT)}}<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, cloze |
| q-18-022 | What does regression testing ensure, and how often in Agile? | That new changes don't break existing functionality; performed frequently in Agile sprints, often automated.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-023 | Order test layers by speed (Unit, Integration, E2E, Performance/Security). | Unit (Very Fast) → Integration (Fast) → End-to-End (Slower) → Performance/Security (Varies).<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, compare |
| q-18-024 | What should you always do before writing the corresponding code? | Define acceptance or behavior-driven (BDD) tests first.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-025 | What runs on each commit in a continuous-testing pipeline? | Build the code, run unit tests, execute integration and smoke tests, generate reports, and optionally deploy to staging.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-026 | Checkout-redesign example: which 3 front-end components were in scope? | CheckoutPage, PaymentOptions widget, responsive layout styles.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-027 | Checkout-redesign example: what were the time estimates? | ~16 hours design/implementation + ~8 hours cross-browser and mobile-responsiveness testing.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-028 | Checkout-redesign example: which dependencies and risks were flagged? | Dependencies: icon-library version, localization pipeline. Risks: order-total-display regression, accessibility compliance.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, apply |
| q-18-029 | What document is produced before CCB approval, and what 4 things does it contain? | The Impact Analysis Report: code-change scope, schedule impact, key test requirements, mitigation steps (e.g., feature-flag rollout).<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, recall |
| q-18-030 | "Submit Payment" button move: should the change be done? | Yes, but with caution and proper planning — risks are moderate and mitigable, impact is isolated to the frontend/UI layer (no backend/business-logic change).<br><sub>(source: m4-ex-impact)</sub> | topic::change-management-continuous-validation, apply |
| q-18-031 | "Submit Payment" move: give 3 conditions to proceed. | Update all automated tests before deploying; conduct usability testing with real users/personas; include accessibility verification (keyboard nav + screen reader); communicate to support/docs/training.<br><sub>(source: m4-ex-impact)</sub> | topic::change-management-continuous-validation, apply |
| q-18-032 | "Submit Payment" move: when should you delay/reconsider? | If analytics show users already succeed with the current flow; if close to a critical release window; if the change is based on assumption rather than user data.<br><sub>(source: m4-ex-impact)</sub> | topic::change-management-continuous-validation, apply |
| q-18-033 | Decision principle for whether a change is worth making? | Not just whether it's "safe," but whether the value it brings outweighs the effort and risk.<br><sub>(source: m4-ex-impact)</sub> | topic::change-management-continuous-validation, recall |
| q-18-034 | Continuous testing vs traditional end-of-cycle testing? | Continuous = tests in every phase, per commit/PR/build, fail-fast feedback. Traditional = testing as a discrete end-of-cycle phase, late feedback.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, compare |
| q-18-035 | Impact analysis vs the change control process — relationship? | Impact analysis is phase 3 inside the 6-phase change control process; it produces the evidence the CCB uses to decide at phase 4.<br><sub>(source: m4-change)</sub> | topic::change-management-continuous-validation, compare |
