# Flashcards — Documenting System Architecture

| ID | Front | Back | Tags |
|---|---|---|---|
| q-14-001 | The four core architecture artifacts and what each captures | BDD = components + relationships; IBD = internal parts + interactions; ICD = detailed interface definitions; FFBD = function sequencing + control logic<br><sub>(source: m3-review)</sub> | topic::documenting-architecture, recall |
| q-14-002 | A {{c1::BDD (Block Definition Diagram)}} gives a structured, hierarchical view of a system's components, properties, and relationships | (define system structure)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, cloze |
| q-14-003 | BDD vs. IBD — the core distinction | A BDD defines *what* components exist; an IBD defines *how* the parts of one block interact internally (data/signal/physical flow)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, compare |
| q-14-004 | BDD connector for a whole-part (essential) relationship | {{c1::Composition}} — a filled (black) diamond<br><sub>(source: m3-document; source: master-notes)</sub> | topic::documenting-architecture, cloze |
| q-14-005 | BDD connector for inheritance/specialization | {{c1::Generalization}} — a hollow triangle arrow<br><sub>(source: master-notes; source: m3-document)</sub> | topic::documenting-architecture, cloze |
| q-14-006 | BDD connector for a loose "has-a"/"uses" link | {{c1::Association}} — a plain line<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, cloze |
| q-14-007 | The four BDD features (per the lecture table) | Blocks (system elements), Associations (relationships), Generalization (inheritance), Value properties (quantitative features)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-008 | The five steps to create an IBD | 1) Choose a block to decompose, 2) define internal parts, 3) show connections, 4) add ports, 5) use a modeling tool<br><sub>(source: m3-document; source: master-notes)</sub> | topic::documenting-architecture, recall |
| q-14-009 | Standard port vs. flow port | Standard port = interface-based interaction (e.g., a sensor/service query); flow port = exchange of material, energy, or data (e.g., battery energy)<br><sub>(source: m3-document; source: master-notes)</sub> | topic::documenting-architecture, compare |
| q-14-010 | Which block does the ADR IBD decompose, into which two internal parts? | The Navigation System block → GPS Receiver + IMU (Inertial Measurement Unit)<br><sub>(source: m3-document; source: master-notes)</sub> | topic::documenting-architecture, apply |
| q-14-011 | An {{c1::ICD (Interface Control Document)}} is a formal document defining/managing interactions between systems, subsystems, or components | (data exchange, protocols, electrical/mechanical connections)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, cloze |
| q-14-012 | The six sections of an ICD, in order | 1) Overview, 2) Interface description, 3) Data exchange details, 4) Communication protocols, 5) System constraints & assumptions, 6) Version control & change management<br><sub>(source: m3-document; source: m3-icd)</sub> | topic::documenting-architecture, recall |
| q-14-013 | An ICD's "Interface description" covers which three interface kinds? | Physical (connectors, wiring, hardware), data (formats, protocols, message structures), and software (APIs, function calls, data exchange methods)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-014 | ADR ICD — Sensor Module → Navigation System: protocol, format, update frequency | Protocol I2C, format JSON, update frequency 10 Hz<br><sub>(source: m3-icd)</sub> | topic::documenting-architecture, apply |
| q-14-015 | ADR ICD — Navigation System → Motor Control Unit: protocol, format, update frequency | Protocol CAN Bus, format Binary, update frequency 100 Hz<br><sub>(source: m3-icd)</sub> | topic::documenting-architecture, apply |
| q-14-016 | ADR ICD — Communication Module → Cloud Server: protocol, format, update frequency | Protocol MQTT, format JSON, update frequency every 30 seconds<br><sub>(source: m3-icd)</sub> | topic::documenting-architecture, apply |
| q-14-017 | ADR ICD error handling per protocol | CRC checks on CAN Bus messages; retries on MQTT communication failures<br><sub>(source: m3-icd)</sub> | topic::documenting-architecture, apply |
| q-14-018 | The ADR FFBD sequence | Start motors → Scan Area → Navigate to destination → Deliver the payload → Return → Stand-by<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-019 | The three elements of an FFBD | Blocks (operations/steps), arrows (control flow/order), branches/loops (conditional/iterative behavior)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-020 | What each stakeholder type needs from architecture docs | Engineers: technical detail/interfaces/dependencies; Project Managers: scope/responsibilities/timelines; Customers/Users: high-level views/capabilities/benefits; QA/Test: traceability/functional-physical breakdown<br><sub>(source: m3-document; source: m3-review)</sub> | topic::documenting-architecture, compare |
| q-14-021 | Why is the BDD called a "living document"? | As requirements and component interactions are refined (esp. in the IBD/ICD phases), new blocks, properties, or connections are added — refinement is normal<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-022 | The four payoffs that make architecture documentation worth its cost | Communication, design validation, traceability, and effective collaboration among stakeholders<br><sub>(source: m3-document; source: m3-review)</sub> | topic::documenting-architecture, recall |
| q-14-023 | The five best practices for documenting architecture for stakeholders | Tailor documentation, clear/concise language, visual representation, traceability, justification<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, recall |
| q-14-024 | New requirement: encrypt cloud traffic. Which artifacts change and how? | Update the Communication Module block in the BDD (add encrypted-communication property) and its interface clauses/version in the ICD (possibly its IBD)<br><sub>(source: m3-document)</sub> | topic::documenting-architecture, apply |
| q-14-025 | ADR ICD — two stated system constraints/assumptions | Battery voltage stays within 20V–25V; Wi-Fi must be available for cloud updates; sensor data latency ≤10 ms (any two)<br><sub>(source: m3-icd)</sub> | topic::documenting-architecture, apply |
