@startuml <SLUG>_StateMachine
' =====================================================================
' State Machine Diagram — STARTER TEMPLATE  (Phase 03, diagram 4 of 7)
' Builds on: the SyRS §9 Modes & States (enumeration + transition table).
' Conventions §7. Replace <PLACEHOLDERS>; delete (example) lines.
' Transition syntax:  event [guard] / action
' DO NOT invent transitions — if Modes & States is incomplete, mark TODO and loop back.
' Use an orthogonal region (concurrent --) for overlays (e.g. a Maintenance region).
' =====================================================================
title <Project Name> — State Machine (TODO)

state "<Initial State>" as InitialState
state "<State A>" as StateA {
}
state "<State B>" as StateB
state "<Fault State>" as Fault

[*] --> InitialState
InitialState --> StateA : <event> [<guard>] / <action>
StateA --> StateB : <event> [<guard>] / <action>
StateB --> StateA : <event> / <action>
StateA --> Fault  : <fault event> / <safe action>
Fault  --> StateA : <recover> [<guard>] / <action>
StateB --> [*]    : <shutdown>

' --- Example transitions (delete) ---
' Idle --> Active : start [authorised] / beginSession
' Active --> Idle : stop / endSession

' --- Orthogonal region overlay (optional — delete if unused) ---
' state Operational {
'   [*] --> Idle
'   --
'   [*] --> NotMaintenance
'   NotMaintenance --> Maintenance : enterMaint [authorised]
'   Maintenance --> NotMaintenance : exitMaint
' }

note right of Fault
  TODO: confirm every SyRS mode/state is covered here;
  no transition exists that the OpsCon does not support.
end note

@enduml
