# Topic: [Stack] gotchas

_Landmines where the model's prior is wrong — not documentation the model already knows.
Write gotchas, not doc summaries. Loaded on demand when working with this part of the stack._

## Current method
- [The correct, current way to do X here.]

## Gotchas (where agents predictably go wrong)
- [e.g. "This framework's router file was renamed in v16; don't use the old `middleware.ts` name."]
- [e.g. "`create view` must set `security_invoker = true` or RLS is bypassed — this has shipped as a bug before."]

## Old patterns (do NOT use — kept so agents recognize and avoid them)
<details><summary>Legacy approach (deprecated [date])</summary>
[What the old way looked like and why it's gone.]
</details>
